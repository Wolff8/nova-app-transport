# PORT: The port the server listens on. Injected automatically by Railway.
# Defaults to 3000 when unset (local development).
PORT="3000"

# NODE_ENV: Set to "production" so the server serves the built frontend from dist/
# instead of starting the Vite dev server. Railway sets this via a service variable.
NODE_ENV="development"
